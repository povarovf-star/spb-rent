# --- scraping (stage 1) ---
curl_cffi>=0.7      # HTTP with a Chrome TLS fingerprint, the key to getting past CIAN's antibot
PyYAML>=6.0

# --- data and features (stages 2-3) ---
pandas>=2.2
pyarrow>=16.0
h3>=4.0

# --- model (stages 4-5) ---
catboost>=1.2
scikit-learn>=1.4

# --- application (stage 6) ---
fastapi>=0.110
uvicorn>=0.29
requests>=2.31

# --- dev ---
pytest>=8.0
ruff>=0.5
